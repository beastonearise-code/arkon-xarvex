from flask import Flask
import os

app = Flask(__name__)

# 🛡️ ఇది రైల్వే కి 100% నమ్మకాన్ని ఇస్తుంది
@app.route('/')
@app.route('/health')
def health():
    # లాగ్స్ లో ఇది కనిపిస్తే రైల్వే సంతృప్తి చెందినట్టు అర్థం
    print("🔱 ARKON: High-Priority Heartbeat Received and Responded!")
    return "OK", 200

if __name__ == "__main__":
    # రైల్వే ఇచ్చే $PORT ని ఖచ్చితంగా వాడాలి
    # ఒకవేళ PORT లేకపోతే 8080 ని డీఫాల్ట్ గా తీసుకుంటుంది
    port = int(os.environ.get("PORT", 8080))
    print(f"🚀 ARKON: Powering up on Port {port}")
    app.run(host='0.0.0.0', port=port, debug=False)
