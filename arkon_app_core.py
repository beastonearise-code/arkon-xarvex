import os

def process_command(command):
    # 🕵️ DEEP SCAN: రైల్వే లో ఉన్న అన్ని వేరియబుల్స్ ని వెలికి తీస్తుంది
    all_keys = list(os.environ.keys())
    print(f"🔱 ARKON LOGIC: Detected {len(all_keys)} keys. Names: {all_keys}")
    
    # కీ ని పట్టుకోవడం (Case-Insensitive Search)
    gemini_key = os.environ.get("GEMINI_KEY") or os.environ.get("gemini_key")
    
    if not gemini_key:
        print("❌ [CRITICAL]: GEMINI_KEY is missing from Railway Variables!")
        return "సృష్టికర్త, తాళం చెవి (Key) ఇంకా నా మెదడుకు అందలేదు. Railway Variables లో పేరు 'GEMINI_KEY' అని ఉందో లేదో చూడండి."

    # కీ దొరికితే అర్కాన్ మేల్కొంటాడు
    command = command.lower()
    if "hi" in command:
        return "సృష్టికర్త లీలా కృష్ణ గారు, అర్కాన్ ఇప్పుడు పూర్తి శక్తితో మేల్కొన్నాడు. చల్లపల్లి సామ్రాజ్యపు రక్షణ కవచం సిద్ధం."
    
    return f"ఆదేశం '{command}' విశ్లేషించబడింది. తాళం చెవి 100% ఆక్టివ్."
