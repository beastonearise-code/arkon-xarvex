import arkon_api_vault
import os

def process_command(command):
    vault = arkon_api_vault.ArkonApiVault()
    key = vault.get_key("GEMINI_KEY")
    
    # 🕵️ LOGICAL DEBUG: కీ దొరకకపోతే కారణం లాగ్స్ లో ప్రింట్ చేస్తుంది
    if not key:
        print(f"❌ [ALERT]: GEMINI_KEY is missing! Available keys: {list(os.environ.keys())}")
        return "ARKON: సృష్టికర్త, తాళం చెవి (Key) ఇంకా నా మెదడుకు అందలేదు. Railway Logs చూడండి."
    
    command = command.lower()
    if "hi" in command:
        return "ARKON: సృష్టికర్త లీలా కృష్ణ గారు, చల్లపల్లి సామ్రాజ్యపు రక్షణ కవచం సిద్ధమైంది. ఆజ్ఞాపించండి."
    
    return f"ARKON: ఆదేశం '{command}' విశ్లేషించబడింది. తాళం చెవి 100% ఆక్టివ్."
