import os

def process_command(command):
    # 🕵️ LOGIC: కీ ని పట్టుకోవడం (Spaces తీసేసి వెతుకుతుంది)
    gemini_key = os.environ.get("GEMINI_KEY", "").strip()
    
    if not gemini_key:
        # లాగ్స్ లో కీస్ లిస్ట్ చూపిస్తుంది
        print(f"⚠️ [ARKON ALERT]: GEMINI_KEY missing! Found: {list(os.environ.keys())}")
        return "ARKON: సృష్టికర్త, తాళం చెవి (Key) అందడం లేదు. రైల్వే Variables లో 'GEMINI_KEY' అని సరిగ్గా ఉందో లేదో చూడండి."

    command = command.lower()
    if "hi" in command or "hai" in command:
        return "ARKON: సృష్టికర్త లీలా కృష్ణ గారు, చల్లపల్లి సామ్రాజ్యపు రక్షణ కవచం సిద్ధమైంది. ఆజ్ఞాపించండి."
    
    return f"ARKON: ఆదేశం '{command}' విశ్లేషించబడింది. తాళం చెవి ఆక్టివ్."
